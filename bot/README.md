
# Гайд по написанию кода

Друзья, бот основан на идее **FSM** (финит стейт машин, конечный автомат). Как я и люблю, это означает что использование фсм доведено до безумия, насколько это я нашел возможным.

---

## Небольшой воркфлоу как пример

1. **Апдейт поступает в `bot.go`** — он достает из БД юзера (или создает нового), отправляет юзера и сам апдейт в `router.go` →

2. **`router.go` создает FSM** — создается машина состояний с передачей функции `getHandler`, которая возвращает адаптер хендлера для конкретного состояния →

3. **FSM пробует все доступные переходы** — для текущего состояния FSM перебирает все возможные события (`machine.AvailableTransitions()`) →

4. **Коллбек `before_event` (в `fsm/fsm.go`)**:
   - Получает хендлер **текущего** состояния через `getHandler(currentState)`
   - Вызывает `LeaveState()` у этого хендлера
   - `LeaveState()` проверяет апдейт и возвращает `Event` (событие для перехода) или пустую строку
   - Если event пустой → вызывается `BadLeaveState()` и переход отменяется (`e.Cancel()`)
   - Если event не совпадает с текущим событием → переход отменяется
   - Если всё ОК → переход разрешается

5. **Коллбек `enter_state` (в `fsm/fsm.go`)**:
   - Получает хендлер **нового** состояния через `getHandler(newState)`
   - Вызывает `EnterState()` у хендлера нового состояния
   - Вызывает `onStateUpdate()` для обновления состояния в БД

**Ключевой момент**: все переходы и вызовы хендлеров управляются автоматически через коллбеки FSM. `router.go` только создает машину и пробует события, вся логика переходов инкапсулирована внутри FSM.

Для полного понимания достаточно глянуть `fsm/` директорию в `internal/` и доку к [looplab/fsm](https://github.com/looplab/fsm)

---

## Что это означает для нас простыми словами?

Любое действие юзера и ответ на него от бота → определенное состояние.

- **Пример 1**: `start_handler.go` возвращает клаву с кнопкой перехода в категории. Когда пользователь нажимает на кнопку:
  - FSM вызывает `LeaveState()` у `start_handler.go`
  - Хендлер проверяет нажатие кнопки и возвращает событие `StartToCategories`
  - FSM выполняет переход в состояние `Categories`
  - FSM вызывает `EnterState()` у `categories_handler.go`
  - Обработка продолжается в новом хендлере

- **Пример 2**: Юзер создается при первом сообщении с состоянием `NewUser`:
  - `LeaveState()` у `new_user_handler.go` возвращает событие `NewUserWelcome`
  - FSM переходит в состояние `NewUserWelcomed`
  - `EnterState()` у `new_user_welcomed_handler.go` отправляет приветственное сообщение
  - После этого может произойти переход в состояние `Start`

**Важно**: 
- `LeaveState()` вызывается у хендлера **старого** состояния (откуда уходим)
- `EnterState()` вызывается у хендлера **нового** состояния (куда приходим)
- Вся логика переходов управляется коллбеками внутри FSM

---

## Работа с БД

В качестве бд юзаем **Postgres**. А конкретно: `pgx` + `sqlc` + `golang migrate`.

**[Небольшая дока по голанг миграте](docs/MIGRATIONS.md)**

1. Чтобы сделать миграцию, измените в папке `db/schema.sql` схему бд (пожалуйста, не удаляйте имеющиеся поля без задавания соответсвующего вопроса всем участникам Хакатонщиков)
2. Миграции ранятся автоматически при вызове конструктора репозитория.
3. Далее, после миграции. Создавайте запросы к бд в `db/sqlc/quries`. После можно вызвать `sqlc generate`, эта хрень нагенерит `internal/db/sqlc`. Гляньте что там и на основе этого можете писать свои сервисы и репозитории.
4. `/internal/repository` для работы с бд, `/internal/service` с бизнес логикой.
5. Создавайте отдельно модель в `internal/model` для работы с бизнес логикой (делайте мапер с сущности бд на модель). Даже если это кажется излишним, все равно делайте.

---

## REST API для карты волонтёров

HTTP-сервер теперь запускается вместе с ботом (см. `cmd/bot/main.go`) и предоставляет эндпоинт `GET /api/v1/map/events`, который возвращает ближайшие мероприятия с учётом радиуса и категорий.

### Переменные окружения

| Переменная | Назначение | Значение по умолчанию |
|------------|------------|------------------------|
| `HTTP_ADDR` | адрес и порт HTTPS-сервера | `:8443` |
| `HTTP_READ_TIMEOUT` | таймаут чтения запроса | `10s` |
| `HTTP_WRITE_TIMEOUT` | таймаут записи ответа | `15s` |
| `HTTP_SHUTDOWN_TIMEOUT` | время на graceful shutdown | `10s` |
| `HTTP_TLS_CERT_FILE` | путь к TLS-сертификату (обязательно) | — |
| `HTTP_TLS_KEY_FILE` | путь к приватному ключу (обязательно) | — |
| `HTTP_ALLOWED_ORIGINS` | список origin'ов через запятую; поддерживает `*` | `https://*.vercel.app`, `https://maxapp.ru`, `https://web.maxapp.ru`, `https://messenger.max.ru` |

Сервер стартует только в HTTPS-режиме, поэтому предоставьте валидную пару сертификат/ключ (например, от Let's Encrypt).

### Переменные окружения для аутентификации

| Переменная | Назначение | Значение по умолчанию |
|------------|------------|------------------------|
| `AUTH_JWT_SECRET` | секрет для подписи JWT-сессий | — (обязательно) |
| `AUTH_MAX_AGE` | сколько времени initData считается свежим | `1h` |
| `AUTH_SESSION_TTL` | срок жизни выданного JWT | `24h` |

`AUTH_MAX_AGE` и `AUTH_SESSION_TTL` принимают значения в формате `time.ParseDuration` (`10m`, `1h30m`, `48h`).

### Эндпоинт `GET /api/v1/map/events`

Параметры:

- `lat`, `lon` — обязательные координаты в градусах.
- `radius_km` — обязательный радиус поиска в километрах (> 0).
- `limit`/`offset` — пагинация (по умолчанию 50 / 0, максимум 100).
- `category_id` (множество параметров) либо `categories` (через запятую) — фильтрация по категориям.

Ответ содержит список `MapEvent` (см. `internal/model/map_event.go`) с полями `distanceKm`, `slotsLeft`, `categoryName` и т.д., а также метаданные пагинации. Возвращаются только мероприятия со статусом `open` и доступными слотами.

Пример запроса:

```
curl "https://example.com/api/v1/map/events?lat=55.75&lon=37.61&radius_km=15&category_id=1&category_id=2"
```

### Эндпоинт `GET /api/v1/map/users/:userID/events`

Возвращает те же `MapEvent`, но только для мероприятий, в которые конкретный волонтёр отправлял заявки. Дополнительно поле `applicationStatus` показывает состояние его заявки (`pending`, `approved`, `rejected`, `cancelled`).

Параметры запроса такие же, как у общей ручки карты (`lat`, `lon`, `radius_km`, `limit`, `offset`, `category_id`). Плюс path-параметр `userID` — идентификатор волонтёра (совпадает с Telegram user id).

> 🔒 Ручка требует заголовок `Authorization: Bearer <JWT>`, полученный через `POST /api/v1/auth/session`. Значение `userID` обязано совпадать с `id` внутри токена; чужие данные недоступны.

Рекомендуемый запрос:

```
curl "https://example.com/api/v1/map/users/123456/events?lat=59.93&lon=30.31&radius_km=30"
```

Ответ пригоден, чтобы подсветить на карте точку, куда пользователь уже подал заявку, где его отклонили и т.д. Даже если событие закрыто или закончено, оно попадёт в эту выборку (пока оно лежит в радиусе фильтра).

### Эндпоинт `GET /api/v1/users/:userID/location`

Возвращает последнюю сохранённую геолокацию пользователя: широту, долготу и время обновления координат.

- 🔒 Требует заголовок `Authorization: Bearer <JWT>`.
- Значение `userID` в path должно совпадать с `id` внутри токена — запрашивать чужие координаты нельзя, иначе придёт 403.
- При отсутствии сохранённого `location_lat`/`location_lon` возвращает 404 с сообщением `у пользователя нет сохранённой геолокации`.

Формат ответа:

```json
{
  "userId": 123456789,
  "lat": 59.9386,
  "lon": 30.3141,
  "updatedAt": "2025-11-14T12:34:56Z"
}
```

Пример запроса:

```
curl "https://example.com/api/v1/users/123456789/location" \
  -H "Authorization: Bearer <JWT>"
```

Ручка удобна, если фронту нужно подсветить текущую точку пользователя на карте или обновить центр/радиус поиска перед подгрузкой событий.

### Эндпоинт `POST /api/v1/auth/session`

Принимает `initData` (строка, которую веб-приложение получает от MAX WebApp bridge), валидирует подпись и свежесть, после чего возвращает короткоживущий JWT. Формат запроса:

```json
{
  "initData": "auth_date=...&user=...&hash=..."
}
```

Успешный ответ:

```json
{
  "token": "eyJhbGciOiJIUzI1NiIs...",
  "expiresIn": 86400,
  "user": {
    "id": 123456789,
    "first_name": "Иван",
    "last_name": "Петров",
    "username": "volunteer",
    "language_code": "ru"
  }
}
```

`expiresIn` — кол-во секунд до истечения сессии. Токен нужно передавать в `Authorization: Bearer <token>` для личных ручек (например, `/api/v1/map/users/:userID/events`).

Пример запроса:

```
curl -X POST "https://example.com/api/v1/auth/session" \
  -H "Content-Type: application/json" \
  -d '{"initData":"auth_date=...&query_id=...&user=...&hash=..."}'
```

### Как ручка используется фронтендом

Фронтовое приложение вызывает эндпоинт при каждом перемещении карты или изменении фильтров. Поток выглядит так:

1. Храним последние координаты центра и радиус видимой области.
2. При drag/zoom рассчитываем новый радиус и шлём запрос с теми же параметрами (`lat`, `lon`, `radius_km`).
3. На клиенте храним `etag`/`lastUpdatedAt` (если нужен кеш) и список категорий; доп фильтры передаём через `category_id`.
4. При успешном ответе заменяем пины на карте и подгружаем детальный попап по `event.id`.

Ответ (усечённый реальный пример):

```json
{
  "data": [
    {
      "id": "6db6c682-2f46-46b6-9a98-93f160e29a0c",
      "title": "Экодесант в Юнтолово",
      "description": "Сбор мусора и сортировка",
      "latitude": 59.9935,
      "longitude": 30.1771,
      "distanceKm": 4.2,
      "categoryId": 2,
      "categoryName": "Экология",
      "slotsTotal": 15,
      "slotsLeft": 7,
      "volunteerCount": 8,
      "startsAt": "2025-11-14T09:00:00Z",
      "endsAt": "2025-11-14T13:00:00Z",
      "chat": "https://t.me/max_volunteers_chat",
      "applicationStatus": "approved",
      "media": [
        {
          "url": "https://cdn.max.ru/event-media/eco-01.jpg",
          "type": "image"
        }
      ]
    }
  ],
  "meta": {
    "limit": 50,
    "offset": 0,
    "total": 4
  }
}
```

Что важно для фронта:

- `distanceKm` уже посчитан на стороне сервера (Haversine) – не нужно пересчитывать.
- `slotsLeft` и `volunteerCount` позволяют подсвечивать почти заполненные события.
- `categoryName` и `categoryColor` (если добавите в БД) помогают отдавать легенду без второго запроса.
- `chat`, `cancelledReason`, `completedAt` пригодятся для диалогов/статусов карточки.
- `applicationStatus` приходит только из пользовательской ручки и позволяет подсветить состояние заявки.
- Ошибки возвращаются в JSON c `error.code` и `error.message`; 400 — валидация параметров, 422 — бизнес-валидация.

Таким образом ручка полностью покрывает потребности карты: все вычисления (радиус, фильтрация по слотам, сортировка по дистанции) происходят на бэке, фронт только визуализирует ответ.

## Моковые данные для фронтенда

Чтобы фронт быстро увидел карту, используйте bash-скрипт `scripts/seed_mock_data.sh`, который через `psql` создаёт категории, пользователей, организаторов, волонтёров и события вокруг Петербурга.

### Локальный запуск

1. Запустите Postgres (например, `docker compose up`) и убедитесь, что `DATABASE_URL` в `.env` указывает на нужную БД.
2. Выполните:

   ```bash
   ./scripts/seed_mock_data.sh
   ```

Скрипт прочитает `.env`, очистит старые сиды через временную таблицу и зальёт актуальные данные. Повторный запуск безопасен — значения просто перезапишутся.

### Запуск внутри контейнера Postgres

Если `psql` не установлен локально, можно прогнать скрипт прямо в `maxbot_postgres`:

```bash
docker cp ./scripts/seed_mock_data.sh maxbot_postgres:/tmp/seed_mock_data.sh
docker exec maxbot_postgres chmod +x /tmp/seed_mock_data.sh
docker exec \
  -e DATABASE_URL='postgres://postgres:postgres@localhost:5432/maxbot?sslmode=disable' \
  -e ENV_FILE='' \
  maxbot_postgres /tmp/seed_mock_data.sh
```

Важные параметры: `DATABASE_URL` обязателен даже внутри контейнера (используйте localhost:5432), а `ENV_FILE=''` отключает повторную загрузку `.env`.

Скрипт сам создаёт связи (медиа, участники, счётчики), поэтому его удобно запускать перед демонстрациями, чтобы откатить тестовую базу к известному состоянию.

---
